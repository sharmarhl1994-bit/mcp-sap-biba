"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.AxiosHttpClient = void 0;
const axios_1 = __importStar(require("axios"));
const AdtHTTP_1 = require("./AdtHTTP");
const utilities_1 = require("./utilities");
const toAxiosConfig = (options) => {
    const config = {
        method: options.method || "GET",
        url: options.url,
        headers: options.headers || {},
        params: options.qs,
        httpsAgent: options.httpsAgent,
        timeout: options.timeout,
        auth: options.auth,
        data: options.body,
        adtRequestNumber: options.adtRequestNumber,
        adtStartTime: options.adtStartTime
    };
    return config;
};
const convertheaders = (raw) => {
    let headers = {};
    if (raw instanceof axios_1.AxiosHeaders) {
        for (const [key, value] of raw) {
            if (value instanceof axios_1.AxiosHeaders)
                headers = { ...headers, ...convertheaders(value) };
            else
                headers[key] = value;
        }
    }
    for (const k in Object.keys(raw)) {
        const val = raw[k];
        if (val instanceof axios_1.AxiosHeaders)
            headers = { ...headers, ...convertheaders(val) };
        else
            headers[k] = val;
    }
    return headers;
};
const axiosRespToHttp = (raw) => {
    const { data, status, statusText, headers } = raw;
    const body = data ? ((0, utilities_1.isString)(data) ? data : `${data}`) : "";
    return {
        body,
        status,
        statusText,
        headers: convertheaders(headers)
    };
};
class AxiosHttpClient {
    constructor(baseURL, config) {
        this.baseURL = baseURL;
        this.config = config;
        const conf = toAxiosConfig({ ...config });
        this.axios = axios_1.default.create({ ...conf, baseURL });
    }
    async request(options) {
        try {
            const config = toAxiosConfig(options);
            const raw = await this.axios.request(config);
            return axiosRespToHttp(raw);
        }
        catch (error) {
            if (!(0, axios_1.isAxiosError)(error)) {
                const message = (0, utilities_1.hasMessage)(error)
                    ? error.message
                    : "Unknown error in HTTP client";
                throw new AdtHTTP_1.HttpClientException(message, undefined, undefined, this.config, options, undefined, error);
            }
            const response = error.response && axiosRespToHttp(error.response);
            throw new AdtHTTP_1.HttpClientException(error.message, error.code, error.status, this.config, options, response, error);
        }
    }
}
exports.AxiosHttpClient = AxiosHttpClient;
